services:
  - type: web
    name: backend-api
    env: docker
    dockerfilePath: ./backend_api/Dockerfile
    
  - type: web
    name: frontend
    env: docker
    dockerfilePath: ./client/Dockerfile
    rewrites:
      - source: /*
        destination: /index.html