services:
  - type: web
    name: backend-api
    env: docker
    dockerfilePath: ./backend_api/Dockerfile
    envVars:
      - key: PORT
        value: 8000
  - type: static
    name: frontend
    dockerfilePath: ./client/Dockerfile
    rewrites:
      - source: /*
        destination: /index.html